# Google Analytics 4 measurement ID (https://analytics.google.com → Admin →
# Data Streams → Web). Leave unset to disable analytics entirely (default).
# Build-time only: `VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX npm run build`.
VITE_GA_MEASUREMENT_ID=
